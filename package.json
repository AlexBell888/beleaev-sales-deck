{
  "name": "cited-agency-site",
  "version": "2.0.0",
  "private": true,
  "description": "Cited. — UK's first GEO agency website. Multi-page static site on Railway.",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
